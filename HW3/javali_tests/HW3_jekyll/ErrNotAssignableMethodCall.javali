// Not_Assignable: The left-hand side of an assignment must be assignable (e.g., not this).
// The only assignable expressions are variables (x), fields (expr.f), and array-indexing (expr[i]).

class Main {
    void main() {
        f() = true;
    }

    boolean f() {
        return true;
    }
}